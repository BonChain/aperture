{
  "name": "@aperture/core",
  "version": "0.1.0",
  "private": true,
  "description": "Aperture core — isomorphic crypto helpers (Story 1.1a: minimal crypto/ only).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./crypto": "./src/crypto/index.ts",
    "./proof": "./src/proof/proofAdapter.ts"
  },
  "scripts": {
    "build:crypto": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@noble/curves": "^2.2.0",
    "@noble/hashes": "^2.2.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^4.1.0"
  }
}
