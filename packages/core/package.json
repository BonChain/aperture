{
  "name": "@aperture/core",
  "version": "0.1.0",
  "private": true,
  "description": "Aperture core — isomorphic crypto helpers (Story 1.1a: minimal crypto/ only).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./crypto": "./src/crypto/index.ts"
  },
  "scripts": {
    "build:crypto": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^4.1.0"
  }
}
