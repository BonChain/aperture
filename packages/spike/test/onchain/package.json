{
  "name": "@aperture/spike-onchain",
  "version": "0.1.0",
  "private": true,
  "description": "Aperture SPIKE-1 on-chain test (Story 1.1c). Lives outside packages/spike/src to preserve the 1.1b import-discipline hard rule (no @mysten/* in the spike layer). Uses @mysten/sui to call aperture::verifier::verify on devnet against the committed golden proof fixture.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:onchain": "vitest run"
  },
  "dependencies": {
    "@aperture/spike": "workspace:*",
    "@mysten/sui": "^2.17.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^4.1.0"
  }
}
