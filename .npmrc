# pnpm config — Aperture
# Pin: vendor/contra @ a0ec1e08996074b11ade2a1ede035cf5a108d291 (HEAD of main, 2026-06-20)
# ts-sdk version pinned to 1.0.0 (paired with the Move git sha above).
# Mirror in: vendor/contra/PINNED_VERSION  +  move/Move.toml `rev`
# Anti-drift: any of the three pin locations changing must update all three atomically.

strict-peer-dependencies=false
auto-install-peers=true
node-linker=isolated
